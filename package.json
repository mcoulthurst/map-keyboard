{
  "name": "openlayers",
  "version": "1.0.0",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "deploy": "vite build --base=map",// sets the output path in index.html
    "serve": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.0.4"
  },
  "dependencies": {
    "ol": "latest"
  }
}
